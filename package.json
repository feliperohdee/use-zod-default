{
	"name": "@simpleimg/zod-default-instance",
	"version": "1.0.0",
	"main": "dist/index.js",
	"license": "MIT",
	"dependencies": {
		"zod": "^3.23.8"
	},
	"files": [
		"dist"
	],
	"devDependencies": {
		"@types/lodash": "^4.17.12",
		"@types/node": "^22.7.7",
		"prettier": "^3.3.3",
		"typescript": "^5.6.3",
		"vitest": "^2.1.3"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"lint": "prettier --write . && yarn tsc",
		"npm-publish": "./.test.sh --run && yarn build && yarn version --patch --no-git-tag-version && yarn publish --non-interactive",
		"test": "vitest"
	}
}
